# Base URL of your Dashbrr instance (no trailing /api)
DASHBRR_URL=https://dashbrr.example.com

# Session token (dashbrr_user_session cookie value) sent as `Authorization: Bearer <token>`.
# Leave empty if the instance runs with DASHBRR_AUTH_BYPASS=true.
DASHBRR_API_KEY=
